import { FeaturePlaceholderShell } from '../components/main/FeaturePlaceholderShell';

export default function ScannerScreen() {
  return (
    <FeaturePlaceholderShell
      eyebrow="Scanner"
      title="Barcode scan "
      titleAccent="coming soon"
      subcopy="Quick add and consume flows will return with the new scanner experience."
      variant="tab"
    />
  );
}
